{
  "name": "ride-cancellation",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:api": "python ml-service/app.py",
    "build": "npm run build --prefix frontend"
  }
}
